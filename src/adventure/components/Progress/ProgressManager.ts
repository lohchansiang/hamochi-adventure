import { Scene } from "phaser";

export default class ProgressManager{
    static preload( scene: Scene ){
       //
    }

    scene: Scene

    constructor(scene:Scene){
        this.scene = scene

        // Main Progress > Linear

        // Side Progress
    }
}